# Инструкция по деплою: Восстановление станций через админку

## 📋 Обзор

Данная инструкция описывает процесс деплоя функционала восстановления станций на продакшн сервер после тестирования на dev окружении.

## ✅ Что было реализовано

1. **Обновлена модель `StationRollbackLog`** - добавлены поля:
   - `nft_address` - адрес NFT станции
   - `is_restored` - статус восстановления
   - `restored_at` - дата восстановления

2. **Создана миграция** - `0120_stationrollbacklog_restore_fields.py`

3. **Обновлена админка Django** - добавлены два actions:
   - 🔍 **Заполнить NFT адреса из TON API** - автоматически получает NFT адреса
   - 🔄 **Восстановить станцию** - восстанавливает станцию пользователя

4. **Создан management command** - `fill_rollback_nft_addresses.py` для получения NFT адресов через терминал

## 📦 Файлы для деплоя на продакшн

### Обязательные файлы:

1. **`core/models.py`** - обновленная модель StationRollbackLog
2. **`core/admin.py`** - обновленная админка с actions
3. **`core/migrations/0120_stationrollbacklog_restore_fields.py`** - миграция БД
4. **`core/management/__init__.py`** - инициализация management commands
5. **`core/management/commands/__init__.py`** - инициализация commands
6. **`core/management/commands/fill_rollback_nft_addresses.py`** - скрипт для получения NFT адресов

## 🚀 Процесс деплоя на продакшн

### Шаг 1: Подготовка

1. **Проверить, что на тестовом сервере все работает:**
   ```bash
   ssh projects-srv
   cd tbtc_dev
   .venv/bin/python manage.py migrate  # Применить миграцию
   .venv/bin/python manage.py fill_rollback_nft_addresses --dry-run  # Проверить скрипт
   ```

2. **Протестировать в админке:**
   - Открыть админку dev сервера
   - Проверить, что actions видны в StationRollbackLog
   - Протестировать заполнение NFT адресов
   - Протестировать восстановление станции (на тестовых данных)

### Шаг 2: Создать бекап продакшн БД

**⚠️ КРИТИЧЕСКИ ВАЖНО:** Перед деплоем создать бекап базы данных!

```bash
ssh projects-srv
cd tbtc
# Создать бекап БД (команда зависит от вашей настройки)
# Например:
.venv/bin/python manage.py dumpdata > backup_before_rollback_restore_$(date +%Y%m%d_%H%M%S).json
```

### Шаг 3: Копирование файлов на продакшн

**Сервер:** `projects-srv`  
**Директория:** `tbtc` (продакшн)

```bash
# Локально выполнить команды:

# 1. Модель
scp /Users/vladskorobahatko/Dev/TonBitcoin/tonbitcoin_front_test/edit/core/models.py \
    projects-srv:tbtc/core/models.py

# 2. Админка
scp /Users/vladskorobahatko/Dev/TonBitcoin/tonbitcoin_front_test/edit/core/admin.py \
    projects-srv:tbtc/core/admin.py

# 3. Миграция
scp /Users/vladskorobahatko/Dev/TonBitcoin/tonbitcoin_front_test/edit/core/migrations/0120_stationrollbacklog_restore_fields.py \
    projects-srv:tbtc/core/migrations/0120_stationrollbacklog_restore_fields.py

# 4. Management commands
scp /Users/vladskorobahatko/Dev/TonBitcoin/tonbitcoin_front_test/edit/core/management/__init__.py \
    projects-srv:tbtc/core/management/__init__.py

scp /Users/vladskorobahatko/Dev/TonBitcoin/tonbitcoin_front_test/edit/core/management/commands/__init__.py \
    projects-srv:tbtc/core/management/commands/__init__.py

scp /Users/vladskorobahatko/Dev/TonBitcoin/tonbitcoin_front_test/edit/core/management/commands/fill_rollback_nft_addresses.py \
    projects-srv:tbtc/core/management/commands/fill_rollback_nft_addresses.py
```

### Шаг 4: Применить миграцию на продакшн

```bash
ssh projects-srv
cd tbtc
.venv/bin/python manage.py migrate
```

**Проверить результат:**
```bash
.venv/bin/python manage.py showmigrations core | grep 0120
# Должно показать: [X] 0120_stationrollbacklog_restore_fields
```

### Шаг 5: Проверка после деплоя

1. **Проверить, что файлы на месте:**
   ```bash
   ssh projects-srv
   cd tbtc
   ls -lh core/models.py core/admin.py core/migrations/0120_* core/management/commands/fill_rollback_nft_addresses.py
   ```

2. **Проверить синтаксис Python:**
   ```bash
   .venv/bin/python -m py_compile core/models.py core/admin.py core/management/commands/fill_rollback_nft_addresses.py
   ```

3. **Проверить, что админка загружается:**
   - Открыть админку продакшн
   - Проверить, что StationRollbackLog отображается
   - Проверить, что actions видны в списке

### Шаг 6: Заполнение NFT адресов (опционально)

Если нужно заполнить NFT адреса для существующих записей:

```bash
ssh projects-srv
cd tbtc
.venv/bin/python manage.py fill_rollback_nft_addresses --dry-run  # Проверка
.venv/bin/python manage.py fill_rollback_nft_addresses  # Применение
```

Или использовать action в админке:
- Открыть админку → Station Rollback Logs
- Выбрать записи без NFT адресов
- Action: "🔍 Заполнить NFT адреса из TON API"
- Нажать "Go"

## 🔄 Откат (Rollback)

Если что-то пошло не так, можно откатить изменения:

### Откат миграции:

```bash
ssh projects-srv
cd tbtc
.venv/bin/python manage.py migrate core 0119  # Откатить до миграции 0119
```

### Восстановление файлов из бекапа:

```bash
# Восстановить файлы из git или бекапа
cd tbtc
git checkout HEAD -- core/models.py core/admin.py
# Или скопировать из бекапа
```

## ⚠️ Важные замечания

1. **Бекап БД обязателен** - перед применением миграции
2. **Тестирование на dev** - обязательно протестировать перед продакшн
3. **Миграция безопасна** - только добавляет поля, не изменяет существующие данные
4. **Rate limiting** - скрипт получения NFT адресов использует задержки между запросами
5. **Мониторинг** - после деплоя следить за логами и ошибками

## 📝 Чеклист деплоя

- [ ] Протестировано на dev сервере
- [ ] Создан бекап БД продакшн
- [ ] Скопированы все файлы на продакшн
- [ ] Применена миграция на продакшн
- [ ] Проверен синтаксис Python файлов
- [ ] Проверена работа админки
- [ ] Проверены actions в админке
- [ ] Протестировано заполнение NFT адресов (на тестовых данных)
- [ ] Протестировано восстановление станции (на тестовых данных)
- [ ] Документированы результаты тестирования

## 🔗 Связанные документы

- `docs/STATION_ROLLBACK_RESTORE_INSTRUCTION.md` - подробная инструкция по использованию
- `docs/STATION_ROLLBACK_FIX_FINAL.md` - исправление проблемы с откатом станций

## 📅 История деплоя

- **2025-12-24:** Реализация функционала восстановления станций
- **2025-12-24:** Деплой на тестовый сервер (tbtc_dev)
- **Дата продакшн деплоя:** _заполнить после деплоя_

---

**Статус:** Готово к деплою на продакшн после тестирования на dev

