# КРИТИЧЕСКАЯ ПРОБЛЕМА: Массовый откат станций из-за проблем с TON API

## 📅 Что произошло

**Дата:** 24 декабря 2025 года  
**Время:** 09:17:45 - 09:18:12 UTC  
**Длительность:** ~27 секунд  
**Количество откатов:** 261 станция

### Детали отката:
- **Nuclear power plant:** 183 отката
- **Thermonuclear power plant:** 64 отката
- **Dyson Sphere:** 13 откатов
- **Neutron star:** 1 откат

## 🔍 Причина проблемы

### Корневая причина
Массовый откат произошел из-за **проблем с TON API**, который вернул ошибку 500 (Cloudflare Internal Server Error) или неполные данные.

### Техническая детализация

1. **Скрипт:** `t.py`, функция `main_mint()` (строка 309)
2. **Проблемный код:** Строки 318-339
   ```python
   i = 0
   while True:
       pages = list(range(i, i + PAGES))
       results = [async_to_sync(get_nfts)(collection_addr, page) for page in pages]
       
       has_short_page = False
       for data in results:
           all_nfts.extend(data.nft_items)
           logging.info(f"{len(data.nft_items)}")
           if len(data.nft_items) < 1000:
               has_short_page = True
       
       if has_short_page:
           break
       i += PAGES
   
   logging.info(f"FINAL {len(all_nfts)}")
   
   if len(all_nfts) < 1:
       logging.info(f"BREAK")
       return
   ```

3. **Логика проверки станций:** Строки 552-577
   ```python
   logging.info("")
   logging.info("checking nfts")
   for station_nft in StationNFTOwner.objects.filter(nft__isnull=False).exclude(
       nft=""
   ):
       try:
           user = users.get(station_nft.wallet or "")
           if user is None:
               mint_string = ""
           else:
               mint_string = ";".join(user["mint_string"])
           if (
               station_nft.nft not in mint_string
               or nfts_info[station_nft.nft]["nft"].sale is not None
           ):
               Notification.objects.create(
                   user=station_nft.user, notif_type="nft_not_found"
               )
               station_nft.user.reset_station()  # ← ОТКАТ СТАНЦИИ
   ```

### Цепочка событий, приведшая к откату:

1. **TON API вернул ошибку 500** или неполные данные
2. **Запросы упали** без обработки ошибок (нет try/except)
3. **`all_nfts` стал неполным** - не все NFT попали в список
4. **`users` dict стал неполным** - только те пользователи, чьи NFT попали в ответ API
5. **Проверка станций:** Для всех станций, чьи NFT не попали в ответ:
   - `user = users.get(station_nft.wallet)` вернул `None`
   - `mint_string = ""` (пустая строка)
   - `station_nft.nft not in mint_string` = `True`
   - **Вызван `reset_station()`** → станция откачена

### Проблемы в текущем коде:

1. ❌ **Нет обработки ошибок API** - если запрос упал, код продолжает работу
2. ❌ **Нет проверки на полноту данных** - нет валидации, что получены все NFT
3. ❌ **Нет защиты от массового отката** - при проблемах с API все станции откатываются
4. ❌ **Нет rate limiting** - хотя лимит 100 req/s, нет контроля скорости запросов
5. ❌ **Нет retry механизма** - при ошибке API нет повторных попыток
6. ❌ **Нет кэширования** - при ошибке API нельзя использовать предыдущие успешные данные

## 📊 Данные из базы

### Логи откатов:
- **Всего откатов в системе:** 511
- **До 24.12.2025:** единичные откаты (1-2 в день) - нормальное поведение
- **24.12.2025:** массовый откат 261 станции - аномалия

### Модель StationRollbackLog:
```python
class StationRollbackLog(models.Model):
    user = models.ForeignKey(UserProfile, on_delete=models.CASCADE)
    from_station = models.CharField(max_length=50)
    generation_level = models.PositiveSmallIntegerField(null=True, blank=True)
    storage_level = models.PositiveSmallIntegerField(null=True, blank=True)
    engineer_level = models.PositiveSmallIntegerField(null=True, blank=True)
    energy = models.FloatField(null=True, blank=True)
    date = models.DateTimeField(auto_now_add=True)
```

## 🛠️ Что нужно исправить

### 1. Добавить обработку ошибок API

**Файл:** `t.py`  
**Функция:** `main_mint()`  
**Место:** Строки 318-339

```python
# ТЕКУЩИЙ КОД (ПРОБЛЕМНЫЙ):
i = 0
while True:
    pages = list(range(i, i + PAGES))
    results = [async_to_sync(get_nfts)(collection_addr, page) for page in pages]
    
    has_short_page = False
    for data in results:
        all_nfts.extend(data.nft_items)
        logging.info(f"{len(data.nft_items)}")
        if len(data.nft_items) < 1000:
            has_short_page = True
    
    if has_short_page:
        break
    i += PAGES
```

**НУЖНО:**
- Обернуть запросы в try/except
- Обрабатывать ошибки TON API (TONAPIError, 500, timeout)
- Логировать ошибки
- При ошибке - либо retry, либо использовать кэш, либо пропустить проверку

### 2. Добавить проверку на полноту данных

**Проблема:** Нет валидации, что получены все NFT из коллекции.

**Решение:**
- Сохранять ожидаемое количество NFT (из предыдущих успешных запросов)
- Сравнивать полученное количество с ожидаемым
- Если разница > 10% - считать данные неполными
- При неполных данных - НЕ выполнять проверку станций

### 3. Добавить защиту от массового отката

**Проблема:** При проблемах с API все станции откатываются.

**Решение:**
- Перед проверкой станций проверять полноту данных API
- Если данные неполные - пропустить проверку станций
- Логировать предупреждение о пропуске проверки
- Использовать кэш из предыдущего успешного запроса (если есть)

### 4. Добавить rate limiting

**Текущая ситуация:** TON API лимит 100 req/s, но нет контроля в коде.

**Решение:**
- Добавить задержки между запросами (минимум 10ms между запросами)
- Использовать asyncio.sleep для контроля скорости
- Логировать предупреждения при приближении к лимиту

### 5. Добавить retry механизм

**Решение:**
- При ошибке API делать повторные попытки (3-5 раз)
- Экспоненциальная задержка между попытками (1s, 2s, 4s)
- После всех неудачных попыток - использовать кэш или пропустить проверку

### 6. Добавить кэширование успешных данных

**Решение:**
- Сохранять последний успешный ответ API в Redis/БД
- При ошибке API использовать кэш
- Обновлять кэш только при успешном запросе
- Валидировать актуальность кэша (не старше 5-10 минут)

## 📝 Инструкции для исправления

### Шаг 1: Анализ текущего кода
1. Открыть файл `t.py`
2. Изучить функцию `main_mint()` (строки 309-577)
3. Понять логику работы:
   - Запросы к TON API
   - Построение словаря `users`
   - Проверка станций и откат

### Шаг 2: Добавить обработку ошибок
1. Обернуть запросы `async_to_sync(get_nfts)()` в try/except
2. Обрабатывать исключения:
   - `pytonapi.exceptions.TONAPIError`
   - `Exception` (общий случай)
3. При ошибке - логировать и решать: retry или использовать кэш

### Шаг 3: Добавить валидацию данных
1. Сохранять ожидаемое количество NFT (из предыдущих успешных запросов)
2. Сравнивать полученное количество с ожидаемым
3. Если данные неполные - НЕ выполнять проверку станций

### Шаг 4: Добавить защиту от массового отката
1. Перед проверкой станций (строка 552) добавить проверку:
   ```python
   if not is_data_complete(all_nfts):
       logger.warning("API data incomplete, skipping station check to prevent mass rollback")
       return
   ```

### Шаг 5: Добавить rate limiting
1. Добавить задержки между запросами
2. Использовать `time.sleep()` или `asyncio.sleep()`

### Шаг 6: Добавить retry механизм
1. Создать функцию `get_nfts_with_retry()`
2. При ошибке - повторять запрос до 3-5 раз
3. Экспоненциальная задержка между попытками

### Шаг 7: Добавить кэширование
1. Использовать Redis для кэша (уже настроен в settings.py)
2. Сохранять последний успешный ответ API
3. При ошибке API - использовать кэш

### Шаг 8: Тестирование
1. Протестировать с ошибкой API (симуляция)
2. Убедиться, что станции НЕ откатываются при проблемах с API
3. Проверить логирование ошибок
4. Проверить работу кэша

## 🔗 Связанные файлы

- **`t.py`** - основной скрипт с проблемным кодом
- **`core/models.py`** - модель `StationRollbackLog` (строки 1395-1405)
- **`core/models.py`** - метод `reset_station()` (строки 561-597)
- **`core/views.py`** - API endpoint `RollbackStationView` (строки 3004-3025)
- **`logs/t.log`** - логи скрипта (можно найти ошибки API)

## ⚠️ ВАЖНО

1. **Это продакшн** - изменения должны быть осторожными
2. **Нельзя откатывать станции** при проблемах с API
3. **Нужно логировать** все действия для отладки
4. **Тестировать** на dev окружении перед деплоем

## 📋 Чеклист исправлений

- [ ] Добавлена обработка ошибок API (try/except)
- [ ] Добавлена валидация полноты данных
- [ ] Добавлена защита от массового отката
- [ ] Добавлен rate limiting
- [ ] Добавлен retry механизм
- [ ] Добавлено кэширование
- [ ] Добавлено логирование всех действий
- [ ] Протестировано на dev окружении
- [ ] Проверено, что станции НЕ откатываются при проблемах с API

## 🎯 Ожидаемый результат

После исправлений:
1. При ошибке TON API - станции **НЕ откатываются**
2. Используется кэш из предыдущего успешного запроса
3. Ошибки API логируются для анализа
4. При неполных данных - проверка станций пропускается
5. Rate limiting предотвращает превышение лимитов API

---

**Дата создания документа:** 2025-12-24  
**Автор анализа:** AI Assistant  
**Статус:** Требует исправления

